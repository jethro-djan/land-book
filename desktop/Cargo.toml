[package]
name = "desktop"
version = "0.1.0"
edition = "2024"

[features]
debug = ["iced/hot"]

[dependencies]
corelib = { path = "../corelib" }
iced = { version = "0.14.0", features = ["canvas", "advanced"] }
